services:
  clerk:
    environment:
      # Add additional CORS origins for development
      - CORS_ORIGINS=http://localhost:8010,http://localhost:3000,http://localhost:3001,http://localhost:3002,http://localhost:5173
    # Use Uvicorn directly for better WebSocket support
    command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000", "--workers", "1"]